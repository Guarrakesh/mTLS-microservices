{
  "name": "bootstrap",
  "version": "1.0.0",
  "description": "Bootstrap Microservice",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev:watch": "nodemon server.js",
    "start": "node server.js",
    "docker:dev": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml up",
    "docker:prod": "docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d",
    "stop": "docker stop mTLS_microservices_bootstrap"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "express": "^4.16.1",
    "js-yaml": "^4.0.0"
  },
  "devDependencies": {
    "nodemon": "^1.11.0",
    "cross-env": "*"
  }
}
